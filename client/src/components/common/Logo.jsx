import { Typography } from '@mui/material';

const Logo = () => {
  return (
    <Typography fontWeight="700" fontSize="1.7rem">
      <span style={{ color: "#FF5733" }}>Movie</span>  {/* Fiery Red */}
      <span style={{ color: "#FFD700" }}>Paradise</span>  {/* Gold */}
    </Typography>
  );
};

export default Logo;
